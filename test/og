var requests = 0
var og = require('../')

;(function work () {
  og('https://www.youtube.com/watch?v=HvolSr1V3J8')
    .then(result => {
      console.log('result', result)
      console.log('requests', ++requests)
      // work()
    })
    .catch(err => console.log('err', err))
})()
